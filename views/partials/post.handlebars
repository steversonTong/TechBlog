<div class="card px-5 py-4 mx-5 my-4 shadow">
    <header class="card-title m-2">
        <h2>
            <a href="/post/{{id}}">{{title}}</a>
        </h2>
    </header>
    <div class="card-text m-2">
        <p>
            {{content}}
        </p>
    </div>
    <div class="comment m-2">
        <b>{{user.name}}</b> |
        {{format_date date_created}} |
    </div>
</div>